[project]
name = "daydayup"
version = "0.1.0"
description = "Court booking utilities"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
