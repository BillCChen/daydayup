# Multi-pool Booking Progress

As of: 2026-07-15 18:37 CST

## Session log

- Confirmed required local tools and versions.
- Re-read the complete planning-with-files workflow.
- Verified the local, GitHub, and Alibaba Cloud commits are aligned at `d09f5c9`.
- Verified the production Web/scan containers are healthy and production has one enabled user.
- Confirmed the three protected skip-worktree analytics files.
- Created branch `multi_pool` from the verified `main` commit.
- Created the Deep-tier plan, findings, and progress artifacts before code edits.
- Captured SHA-256 baselines for the three protected skip-worktree files.
- The first baseline wrapper reached the test command but its cleanup/status capture stopped because `status` is a read-only zsh variable; the retry will use `test_rc`.
- Re-ran the full baseline with the corrected wrapper: 87 tests passed in 1.524 seconds.
- Confirmed production has sufficient backup/build space and separate repo/state mounts.
- Locked the structured multi-pool result event contract between the engine and Web implementation units.
- Read the live Dockerfile and Compose file; the rollout only needs a Web-service mode environment key and keeps the scan service unchanged.
- Web/API implementation and 10 focused tests are in place and passing.
- Performed an early cross-unit review and sent four concrete engine corrections back before final testing.
- Web/API implementation is complete: 11 focused tests and 11 cancellation/exact compatibility tests pass.
- Main integration pass added dry-run ownership rows to history and revalidated JavaScript plus whitespace checks.
- Added the anchored-neighbor fallback requirement to the engine correction set before convergence.
- Completed the engine coordinator implementation and its 15 focused tests.
- Re-ran the existing fast-booking regression set: 44 tests passed.
- Closed the remaining acceptance gaps with exact one-hour balance preflight and explicit disabled/cardless/underfunded account rejection.
- Added explicit retained-single-success, business-failure, and third-reconciliation-visibility tests.
- Full regression after the added gates: 119 tests passed in 1.542 seconds.
- JavaScript syntax, Python AST, whitespace, generic secret-pattern, and protected-file hash checks passed.
- Started an independent read-only implementation review after all local gates passed.
- Independent review found and blocked release on concurrent multi-pool process starts plus Web-only runtime-mode enforcement.
- Added atomic server-side booking exclusivity for pool jobs and an engine-side `off/dry_run/live` guard.
- Added structured lease acquisition/result, account cooldown, and global cooldown events using only `pool_1/pool_2` identity.
- Replaced serial-only concurrency evidence with thread-barrier lease and process-start tests.
- Added a simulated multi-pool `guided-fast` integration test covering primary-account probing, adjacent submissions, and collector shutdown.
- Focused regression after review fixes: 37 tests passed; full regression: 124 tests passed in 1.546 seconds.
- Requested an independent read-only re-review of all review-driven corrections.
- The re-review closed the original P0/P1 findings and identified a P2 same-account double-lease invariant gap.
- Added `account_slot_in_flight`, strengthened the concurrent owner assertions, and re-ran the full 124-test suite successfully.
- Final independent re-review found no remaining P0-P2 issue; Phase 4 is complete.
- Created four intentional implementation/documentation commits and pushed `origin/multi_pool`.
- Verified the remote branch resolves to `387fad78e2444837d991d4fc64addb4c8cc4526b` before the phase-status closeout commit.

## Current phase

Phase 6 — merge to `main`, publish the release tag, and verify GitHub identity.

## Verification results

- Git worktree was clean before branch creation.
- `multi_pool` did not exist locally or on GitHub before creation.
- GitHub CLI authentication is active for the expected account.
- Engine focused tests: 15 passed.
- Existing fast-booking regression tests: 44 passed.
- Multi-pool focused tests after review fixes: 37 passed.
- Full final-candidate regression: 124 passed.

## Outstanding work

- Publish Git history, merge/tag, and deploy production in dry-run mode.
