# Multi-pool Booking Progress

As of: 2026-07-16 13:55 CST

## Session log

- Confirmed required local tools and versions.
- Re-read the complete planning-with-files workflow.
- Verified the local, GitHub, and Alibaba Cloud commits are aligned at `d09f5c9`.
- Verified the production Web/scan containers are healthy and production has one enabled user.
- Confirmed the three protected skip-worktree analytics files.
- Created branch `multi_pool` from the verified `main` commit.
- Created the Deep-tier plan, findings, and progress artifacts before code edits.
- Captured SHA-256 baselines for the three protected skip-worktree files.
- The first baseline wrapper reached the test command but its cleanup/status capture stopped because `status` is a read-only zsh variable; the retry will use `test_rc`.
- Re-ran the full baseline with the corrected wrapper: 87 tests passed in 1.524 seconds.
- Confirmed production has sufficient backup/build space and separate repo/state mounts.
- Locked the structured multi-pool result event contract between the engine and Web implementation units.
- Read the live Dockerfile and Compose file; the rollout only needs a Web-service mode environment key and keeps the scan service unchanged.
- Web/API implementation and 10 focused tests are in place and passing.
- Performed an early cross-unit review and sent four concrete engine corrections back before final testing.
- Web/API implementation is complete: 11 focused tests and 11 cancellation/exact compatibility tests pass.
- Main integration pass added dry-run ownership rows to history and revalidated JavaScript plus whitespace checks.
- Added the anchored-neighbor fallback requirement to the engine correction set before convergence.
- Completed the engine coordinator implementation and its 15 focused tests.
- Re-ran the existing fast-booking regression set: 44 tests passed.
- Closed the remaining acceptance gaps with exact one-hour balance preflight and explicit disabled/cardless/underfunded account rejection.
- Added explicit retained-single-success, business-failure, and third-reconciliation-visibility tests.
- Full regression after the added gates: 119 tests passed in 1.542 seconds.
- JavaScript syntax, Python AST, whitespace, generic secret-pattern, and protected-file hash checks passed.
- Started an independent read-only implementation review after all local gates passed.
- Independent review found and blocked release on concurrent multi-pool process starts plus Web-only runtime-mode enforcement.
- Added atomic server-side booking exclusivity for pool jobs and an engine-side `off/dry_run/live` guard.
- Added structured lease acquisition/result, account cooldown, and global cooldown events using only `pool_1/pool_2` identity.
- Replaced serial-only concurrency evidence with thread-barrier lease and process-start tests.
- Added a simulated multi-pool `guided-fast` integration test covering primary-account probing, adjacent submissions, and collector shutdown.
- Focused regression after review fixes: 37 tests passed; full regression: 124 tests passed in 1.546 seconds.
- Requested an independent read-only re-review of all review-driven corrections.
- The re-review closed the original P0/P1 findings and identified a P2 same-account double-lease invariant gap.
- Added `account_slot_in_flight`, strengthened the concurrent owner assertions, and re-ran the full 124-test suite successfully.
- Final independent re-review found no remaining P0-P2 issue; Phase 4 is complete.
- Created four intentional implementation/documentation commits and pushed `origin/multi_pool`.
- Verified the remote branch resolves to `387fad78e2444837d991d4fc64addb4c8cc4526b` before the phase-status closeout commit.
- Published the branch closeout commit `c67cc70`, merged to `main` as `9850e07`, and pushed annotated tag `v3.8-multi-pool` at the merge commit.
- Re-ran all 124 tests on merged `main`; all passed.
- Created and checksum-verified the Alibaba Cloud backup at `/opt/huairou/daydayup/backups/20260715_183842_v3_8_multi_pool`.
- Direct server GitHub fetch failed with the known HTTP/2 framing error; a verified complete bundle fast-forwarded the clean checkout to `9850e07`.
- Added Web-only `DAYDAYUP_MULTI_POOL_MODE=dry_run` and validated the Compose configuration.
- Corrected an omitted Compose project name by removing the mistakenly created `daydayup` project and recreating the intended `daydayup-prod` Web/scan containers.
- Verified the duplicate scan container left no persistent-state modification during its approximately 15-second lifetime.
- Verified both production containers at engine `3.8.0`, restart count zero, no recent errors, HTTP 200/401 health behavior, and one Web plus one scan process.
- Production container focused regression: 37 tests passed.
- Production isolated dry-run: two adjacent hours, distinct account slots, 350ms second-account delay, two dry-run ownership events, redaction passed, and zero final-submit HTTP events.
- Confirmed only one production account row currently has the token/session/card context needed for multi-pool; live enablement remains gated on password rotation and Web OAuth onboarding.
- Resumed Phase 8 after the user reported that the page's add-user entry was not visible.
- Confirmed the user form exists but is hidden behind the administrator unlock flow; began comparing the existing Web OAuth route with a local one-shot fallback.
- Located the actual visibility defect: compact CSS hides `.utility-panel` unconditionally, so the user-management section cannot be reached even after following the `#users` navigation link.
- Selected a local one-shot OAuth exchange as the immediate credential path and a narrow CSS repair as the durable usability fix.
- Removed `.utility-panel` from the compact global hidden selector and added a focused frontend regression test.
- Reused the existing `pku-court-booking` Python environment after the system Python lacked `requests`; all 125 tests passed.
- Selected the repaired production Web OAuth route over local credential exchange so the rotated password never needs to cross chat or shell boundaries.
- Independent review found no P0/P1 issue and two P2 scope/test-strength concerns; the repair is now targeted to `#users` only and the test covers both DOM and CSS state.
- Committed and pushed the standalone user-panel repair as `6202f7e`; created and checksum-verified the ECS rollback backup at `/opt/huairou/daydayup/backups/20260716_120000_user_panel`.
- Fast-forwarded production and recreated only the Web container. The scan container retained its original start time; both containers have zero restarts and Web remains in `dry_run` mode.
- Production focused tests pass, ECS-local HTTP/Nginx checks pass, and browser computed style proves only the user utility panel is visible.
- Began Phase 9 after the operator reported stale balance/bookings on account changes and ambiguous availability freshness.
- Confirmed the race: account-scoped responses have no selected-user guard, queued refresh returns before completion, and the old account view is not cleared during switching.
- Implemented request-user guards for status, cards, bookings, booking history, scan tasks, and availability.
- Added immediate account-view clearing, disabled user selection during the forced refresh, availability freshness status/timestamp, and inert stale availability controls during refresh.
- Added a Node VM runtime harness plus frontend contract tests; all four focused frontend tests pass.
- Independent review blocked publication on a false-success refresh path and identified two P2 availability-state issues.
- Replaced fail-fast swallowed refreshes with structured `allSettled` outcomes and per-section failure states; account switches now report partial failure rather than success.
- Reconcile preserved availability selections against the latest snapshot and announce removed stale choices; preserve the freshness chip's dedicated layout class.
- Expanded the runtime harness to cover queued-refresh completion, expired-selection removal, and freshness-class retention; all four focused frontend tests pass.
- Production read-only probes confirmed both enabled user rows currently share the exact same Token and return identical card/booking data.
- Expanded Phase 9 to add duplicate-token save and multi-pool preflight rejection before publication; the second record must be reauthorized with a different WeChat identity.
- Added duplicate-token rejection in both user persistence and multi-pool preflight, plus visible inline user-save success/failure feedback.
- Full regression passes 129 tests; the runtime harness additionally preserves still-valid selections and propagates queued-refresh failure results.
- Final independent review found no P0/P1 issue and one P2 diagnostic-order issue for two empty account Tokens.
- Moved the missing-Token guard before the distinct-Token guard and added a two-empty-Token regression; all 130 tests now pass in 4.530 seconds.
- JavaScript checks, the runtime harness, Python compilation, whitespace checks, protected-file hashes, and the refined added-line security scan all pass.

## Current phase

Phase 9 — publish and deploy the account-scoped refresh/freshness repair. Phase 8 remains gated on reauthorizing the second record with a genuinely different WeChat identity.

## Verification results

- Git worktree was clean before branch creation.
- `multi_pool` did not exist locally or on GitHub before creation.
- GitHub CLI authentication is active for the expected account.
- Engine focused tests: 15 passed.
- Existing fast-booking regression tests: 44 passed.
- Multi-pool focused tests after review fixes: 37 passed.
- Full final-candidate regression: 124 passed.
- User-panel repair full regression: 125 passed.
- Account refresh/freshness final regression: 130 passed.
- Account refresh runtime harness and JavaScript syntax checks: passed.
- Final independent review: no open P0-P2 issue after the missing-Token diagnostic-order correction.

## Outstanding work

- Rotate the disclosed second-account password outside the project.
- Add the intended second account through the Web OAuth flow without persisting its password.
- Run the two-account session/card/balance/order preflight and switch to live only after explicit confirmation.
- Publish and deploy the verified Phase 9 repair while keeping production multi-pool mode at `dry_run`.
