# Booking Reliability Repair Progress

As of: 2026-07-15 14:53 CST

## Status

- [x] Reconfirm local `main` and `origin/main` at `a1cd8c9f61daf1a851108715b94c8422380477cc`.
- [x] Select the bounded-reconciliation and adaptive-backoff route.
- [x] Capture baseline tests and protected-file hashes: 83 tests passed.
- [x] Add regression tests for stable absence, failed reconciliation, unfiltered queries, and cooldowns.
- [x] Implement reconciliation, cooldown, recovery, and detailed logging changes as engine 3.7.0.
- [x] Complete local verification and separate safety-checklist review: 87 tests passed.
- [x] Commit `2488a7e`, tag `v3.7-booking-timeout-recovery`, and push GitHub `main`.
- [x] Create and checksum rollback backup `/opt/huairou/daydayup/backups/20260715_144810_v3_7_0`.
- [x] Transfer commit `2488a7e` with the Git bundle fallback and recreate Web and scan services.
- [x] Complete production verification and record evidence.

## Current bottleneck

None in the current deployment scope. The remaining evidence depends on the next real 12:00 contention run.

## Execution notes

The first SSH probe omitted the established `~/.ssh/huairou-test.pem` identity and failed with `Permission denied (publickey)` without changing production. The key-backed connection is verified. The rollback bundle passed SHA-256 verification. Direct server-side `git fetch` then failed with `curl 16 Error in the HTTP2 framing layer`; the checkout and containers remained unchanged, so deployment switched to the established Git bundle transfer fallback. A first remote bundle verification was run outside a repository and returned `need a repository to verify a bundle`; rerunning it inside the production checkout succeeded. Container process verification first attempted unavailable in-container `ps`; host `/proc` inspection then confirmed the expected Web and scan commands.

## Next live verification

After the next real 12:00 run, inspect reconciliation snapshots, cooldown progression, total remaining window after any timeout, and whether two contiguous hours were confirmed.

## Deployment evidence

- Code release commit: `2488a7e8a1f3ede6a1e6728b235db3a3fc8d4242`.
- Backup: `/opt/huairou/daydayup/backups/20260715_144810_v3_7_0` with verified SHA-256.
- Services: Web and scan up, zero restarts, zero recent error lines.
- HTTP: local upstream 200, ECS-side public HTTPS 200, unauthenticated API correctly 401.
- Runtime: engine 3.7.0 in both containers; success and first `too_fast` gaps both 1.8 seconds.
- Read-only orders: unfiltered query succeeded with two active orders and one expected target match.
- Client-path caveat: the local Mac VPN route resolves the public domain to `198.18.1.60` and fails TLS before HTTP.
