# Daydayup Second-Account OAuth Progress

As of: 2026-07-18 14:06 CST

## Session: 2026-07-18

- Classified the operation as Deep tier because it crosses OAuth credentials and production persistent state.
- Selected the existing local Web OAuth flow with a backed-up, target-scoped production update.
- Established explicit redaction and no-restart boundaries.
- Confirmed the local worktree starts clean on `main`.
- Located and reviewed the current OAuth link, Token exchange, user-store write, and scan-worker read paths.
- Confirmed that final exchange still requires the second booking account's current password; the WeChat redirect code alone cannot complete the account binding.
- Confirmed a production user-file update can be observed by Web and scan without restarting either process.
- Confirmed local/server commit alignment, healthy Web and scan containers, and production user-file mode `0600`.
- Logged the unavailable ECS `python` command; the next inventory probe will use `python3`.
- Re-ran the redacted inventory with `python3` and identified the existing second-user target as `mingyue` / `张明月`.
- Selected the existing project environment for the upstream link request; no dependency or environment mutation is needed.
- Generated and structurally validated the live WeChat authorization link.
- Completed Phase 1 and moved to the user handoff checkpoint.
- Reviewed the atomic production write path and defined the one-shot FIFO handoff plus redacted server-side verification route.
- Created and AST-validated the temporary one-shot exchange/update helper with rollback-on-verification-failure.
- The first launch failed before creating the FIFO because `/tmp` became Python's import root; the helper will explicitly add the current workspace before relaunch.
- Revalidated both local and embedded remote Python source after the import fix.
- Launched the mode-`0600` FIFO listener successfully; it is waiting for the user's local hidden prompt input and will expire after 15 minutes.
- Received the hidden FIFO input, exchanged the OAuth code, checksum-backed up the production user file, and updated only `mingyue`.
- Independently verified the new second-account Token, card/balance, one active booking, file mode, uniqueness, unchanged primary row, unchanged containers, clean server checkout, and public HTTP 200.
- Logged a non-blocking primary direct-order query shape error; the next account-scoping check will use the live Web API.
- The first Web API check used the wrong cookie-auth assumption and returned 401; source inspection identified the required `X-Daydayup-Key` header for the corrected attempt.
- Re-ran the production Web API check with the correct header: status, cards, and bookings each returned user key `mingyue`; balance is `160.00` and active booking count is 1.
- Confirmed the two users have no credential conflict.
- Confirmed the FIFO was removed automatically and deleted the temporary one-shot helper.
- Added a new live production validation scope after the user explicitly authorized one real second-account booking and cancellation through the frontend.
- Selected the exact/single-slot frontend route; direct backend mutation and the duration-based manual route are outside this validation.
- Established hard stops for unknown booking outcomes, ambiguous account ownership, unavailable cancellation, unexpected refund terms, and no tomorrow availability.
- Opened the production frontend in an isolated in-app browser and confirmed it is at the access-password screen with no existing login session.
- Paused at the credential boundary so the user can enter the password directly in the visible page without exposing it to automation.
- The user completed the access-password step.
- The first post-handoff lookup checked the released-tab list, which was empty; a read-only session inventory showed the original production tab is still controlled as tab `1`, so it will be reused directly.
- Reacquired the original production tab without opening or reloading a duplicate.
- Confirmed the authenticated dashboard initially selected `陈启轩`, then switched it through the visible user control to `张明月`.
- Captured the second-account baseline: balance `160.00`; one active cancellable order on 2026-07-20 19:00-20:00, court 10; availability refresh visibly in progress for `张明月`.
- Waited for the account-scoped refresh to finish and identified the only tomorrow candidate: 2026-07-19 09:00-10:00, court 1.
- Verified the exact slot control is unique and non-overlapping using its visible frontend data attributes; no mutation has yet been submitted.
- Clicked the exact slot and verified its frontend preview: `张明月`, 2026-07-19 09:00-10:00, court 1, charge `30.00`, balance `160.00`.
- Clicked the preview's unique `提交预约` button exactly once. The page entered `结果刷新中`; no second submission or backend mutation will be attempted while the result is pending.
- Waited for the automatic result refresh. It completed without showing the new order or a balance change, while the slot remained displayed and selected.
- Checked the frontend browser log for errors/warnings and found none. The outcome remains unknown; the next action is one visible dashboard refresh, not another submission.
- Clicked the visible dashboard `刷新` once. The authoritative refreshed account state still has one old order, balance `160.00`, and the candidate slot available.
- Read the visible page log and warning state: only `刷新完成` is present, with no exact-booking result/error and no warning text.
- Reviewed the shipped frontend handler and confirmed any completed exact-booking API path must log a result or error. Classified the first click as a no-effect UI attempt, not a hidden/delayed order.
- Approved one controlled second UI attempt only after toggling the slot off/on; the next observation must prove the handler enters `提交中` or the test stops.
- Rebuilt the selection entirely through the frontend: deselection removed the preview, and reselection recreated the exact expected preview. No booking mutation occurred during this state repair.
- Performed the single controlled reattempt. The exact-submit button immediately became disabled and displayed `提交中`.
- The frontend page log then reported one exact-booking success and zero failures; the target availability control disappeared under the still-selected `张明月` account.
- Confirmed the completed booking in the second account's visible state: balance `130.00`; new active cancellable row 7/19 09-10, court 1; old 7/20 order unchanged; matching success history entry present.
- Located the exact new row's frontend cancellation control without recording its order identifier.
- Opened the cancellation preview from the new row itself. The dialog unambiguously identifies the just-created 7/19 09-10 court-1 order and offers a full `30.00` refund at 100%.
- Typed the required `CANCEL`, verified the unique confirmation became enabled, and clicked it exactly once.
- The dialog remained visible after the initial 1.2-second wait; no second confirmation will be attempted while the cancellation result is pending.
- Reviewed the frontend cancellation handler and confirmed it deliberately leaves the button visually enabled while awaiting the request; the safe action is to wait for its log/state transition, not re-click.
- The frontend log returned an explicit cancellation failure from the upstream operation. The dialog remains open and the confirmation was not repeated.
- Began a read-only cancellation-path diagnosis focused on account context, Session dependency, just-created-order timing, and upstream parameter construction.
- Re-read the Aliyun host routing guidance and retained its historical/current boundary; the next server evidence will be collected live and redacted.
- Traced the cancellation backend and shared HTTP client. Verified the second account's missing Session removes the cookie only; order lookup, refund preview, and booking still work through Token.
- Marked missing Session as the leading hypothesis for the failed cancellation POST, while keeping affiliate-card and just-created-order timing as alternatives pending live evidence.
- Compared the implementation with the captured-flow guide and Git history. The payload shape is intentional and unchanged; existing tests do not exercise a real Token-only cancellation.
- The first live ECS log probe failed at SSH authentication for the `root` identity. Inspected the SSH host inventory and found no Aliyun alias; the same failed identity will not be retried.
- The one alternate `ubuntu` probe was also denied. Searched project deployment references and found no established SSH wrapper; identity guessing is now stopped.
- Found the established mode-`0600` Huairou ECS key by filename and successfully authenticated without exposing its contents. The first filtered Compose/log output contained no matching lines, so the next probe will enumerate the current container/project names.
- Enumerated live containers and confirmed both Daydayup services are up. Read-only Web logs show the second-account UI continues polling successfully with no crash; broad logs are too noisy for root-cause detail.
- Filtered the Web access log to the three relevant POST routes and confirmed success/success/failure HTTP outcomes in the expected order.
- Verified cancellation cannot reuse any transient cookie from its preceding order lookup because the current implementation constructs separate HTTP clients.
- Performed a redacted read-only live order check: exactly one target order exists and it is already marked cancelled; no Session is stored and the lookup set no cookie.
- Corrected the diagnosis: cancellation succeeded as a side effect but the backend/frontend reported a false failure because non-success responses bypass the existing post-cancel reconciliation.
- Rejected any retry. The only remaining action is to close the stale dialog and use the visible frontend refresh to verify order removal and balance restoration.
- The frontend's own polling refreshed the selected second account: balance restored to `160.00`, the test order disappeared, the original 7/20 order remained, and the released tomorrow slot reappeared.
- Closed the stale failure dialog through the frontend. The restored account state remained visible, completing the real booking-and-cancellation loop.
- Clicked the visible frontend `刷新` once more and confirmed the restored balance/order/availability state did not regress.
- Ran Git whitespace and targeted sensitive-value checks. No tracked source changed; only the three timestamped plan/evidence files are untracked.
- Finalized the isolated browser session after the restored frontend state was confirmed.
- Final runtime/sync safety check passed: Web and scan are running with zero restarts; evidence files are non-empty; no new cache/fragment directory was created.

## Session: 2026-07-18 Repair Follow-up

- The user explicitly requested fixes for the silent exact-submit click and cancellation false-negative behavior.
- Classified the follow-up as Deep tier because it changes live-order outcome semantics and duplicate-submission safety.
- Loaded the product/design contract and selected backend reconciliation plus frontend locks as the primary route.
- Preserved the existing three untracked evidence files and started Phase 9; no source file has been edited yet.
- Traced the availability/exact-submit state machine and found a concrete lock inconsistency plus a silent guard return that explains the observed no-feedback click.
- Reviewed cancellation coverage and confirmed it lacks every ambiguous-outcome branch seen in production.
- Reused the existing product/browser contract: preserve the borderless UI and add explicit pending/disabled/error behavior only.
- Confirmed the aligned refresh cadence is 5 seconds with a 60-second availability TTL, making the live first-click symptom consistent with the identified lock overwrite.
- Selected the existing Node state harness for frontend regression coverage and the captured cancellation fake for backend ambiguous-outcome coverage.
- Added backend regression cases for upstream `fail` with an applied side effect, request error after an applied side effect, third-check visibility, unchanged active order, reconciliation-query failure, and the one-write invariant.
- Ran the new backend cases in the repository's existing `.venv`: five fail against the current implementation for the expected reasons, while the original success and preview cases remain green. This is the required red baseline before implementation.
- Implemented the backend one-write reconciliation loop and made all seven cancellation tests green, including upstream `fail`/timeout after a real side effect and delayed third-check visibility.
- Extended the existing Node harness for the exact-submit refresh lock, visible empty-selection feedback, cancellation pending state, account lock, and duplicate-request prevention. Its red baseline fails at the current refresh-lock behavior as expected.
- Updated exact booking so availability refresh and submission share one authoritative interaction lock; the button now says `分布刷新中` or `提交中`, empty/duplicate guards emit visible feedback, and the original account/slot snapshot is held through result refresh.
- Updated cancellation so the dialog and legacy detail action enter a visible `退订中` state, account switching and repeated writes are blocked, and close/backdrop actions cannot dismiss an in-flight cancellation.
- Added a backend three-check original-account reconciliation loop after the single cancellation write. Upstream `fail` and request errors are accepted only when the order is missing or cancelled; otherwise the original error is preserved.
- Made post-confirmation bookings/cards refresh best-effort so an already confirmed cancellation is not reclassified as failed because an ancillary balance refresh is temporarily unavailable.
- The eight cancellation tests, Node account-state harness, JavaScript syntax check, and Python compile check are green. Full-suite verification remains.
- Extended the pending-state lock to the modal close/back controls so every visible cancellation action is disabled while the single request is in flight.
- Re-ran the complete suite after the final interaction change: all 138 Python tests pass.
- Completed Python AST validation across 20 files, JavaScript syntax checks, `git diff --check`, and targeted sensitive-value scans.
- Completed a local browser pass against a disposable fixture: exact submit showed disabled `提交中`; cancellation showed disabled `退订中`; account switching was locked; the simulated confirmed cancellation closed the dialog and removed the active row.
- Finalized the local browser tab, stopped the fixture server, deleted the temporary fixture, and removed test-created cache directories. Production remained untouched.

## Current State

- **Phase:** complete
- **Next checkpoint:** user decision on commit/push/deployment; no production mutation is authorized by this follow-up.

## Verification Log

| Check | Result |
|---|---|
| Local Git branch/state | `main`, clean, aligned with local `origin/main` tracking ref |
| OAuth exchange | Success; secret values remained in the one-shot process only |
| Production target | Existing `mingyue` row updated; Token changed and remains unique |
| Card verification | `学生球类卡`, balance `160.00` |
| Second active bookings | 1 active booking returned through the new Token |
| State safety | Backup checksum valid; primary and all non-target rows unchanged; mode `0600` |
| Runtime safety | Web and scan IDs/start times/restart counts unchanged |
| Production Web account scope | Status/cards/bookings each returned `mingyue` |
| Frontend account scope | User selector visibly shows `张明月`; balance `160.00`; one active order on 7/20 19-20, court 10 |
| Frontend exact booking | Success log 1/0; balance `130.00`; new active 7/19 09-10 court-1 order and matching success history |
| Frontend cancellation/refund | Upstream response falsely reported failure, but refreshed UI shows balance `160.00`, test order gone, original order retained, slot released |
| Credential conflicts | None for either user |
| Temporary input path | FIFO absent; helper deleted |
| Repair regression suite | 138 tests passed |
| Static checks | Python AST 20 files, all JavaScript syntax, whitespace, and sensitive scans passed |
| Local browser repair contract | `提交中`/`退订中` disabled states and account locks verified; simulated cancellation closed only after confirmation |
| Repair deployment | Not performed; production checkout and containers unchanged |
